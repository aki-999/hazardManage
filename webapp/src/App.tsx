import * as React from 'react';
import * as ReactDOM from 'react-dom/client';
import * as UI from '@mui/material';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <UI.AppBar>
          <UI.Typography>abc</UI.Typography>
        </UI.AppBar>
      </header>
    </div>
  );
}

export default App;
